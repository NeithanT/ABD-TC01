apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deploy/deployment.yaml
configMapGenerator:
  - name: app-env
    envs:
      - .env
  - name: realm-config
    files:
      - ./keycloak/realm-export.json
